<div class="wrapper">
  <div class="container">
    <div class="row">
      <% @posts.each do |post| %>
        <h2><%= post.title %></h2>
        <span><%= post.photo %></span>
        <span><%= post.category %></span>
      <% end %>
    </div>
  </div>
</div>
